# Firebase configuration is in src/integrations/firebase/client.ts
# These environment variables are no longer needed after migration to Firebase

# Telegram Bot Token (set via Firebase Secrets for Cloud Functions)
# Run: firebase functions:secrets:set TELEGRAM_BOT_TOKEN
